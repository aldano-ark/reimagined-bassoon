# Read-only design library

`:core:designsystem` points to the template's existing source directory. Its generated output is redirected into this example's `build/designsystem/`; the example does not modify template sources or build files.
